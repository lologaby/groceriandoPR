# Production Environment Variables for Vercel
# Copia estos valores a Vercel → Settings → Environment Variables

# Backend API URL (Railway o Render)
VITE_API_URL=https://groceriando-backend.railway.app

# O si prefieres usar el proxy de Vercel (vercel.json)
# No necesitas esta variable si usas rewrites en vercel.json
